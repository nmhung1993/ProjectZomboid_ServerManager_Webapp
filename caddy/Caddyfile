{
	# Managed by setup.ps1 -- edit freely or re-run .\make.ps1 init
}

:443 {
	tls /etc/caddy/certs/cert.pem /etc/caddy/certs/key.pem
	reverse_proxy app:8000
}

:80 {
	redir https://{host}{uri} permanent
}